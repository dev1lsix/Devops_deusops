# Лабораторная работа №2

## Билд и запуск

Для билда и запуска контейнеров выполните команду:

```bash
cp .env.example .env && docker compose up -d --build
```

### Запущенный Django будет доступен по адресу http://localhost:8000/
